server:
  port: 8080

# spring properties
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://database-2.ctw0mxzubbyd.ap-south-1.rds.amazonaws.com:3306/SSG?serverTimezone=Asia/Seoul
    username: admin
    password: admin1234
  mvc:
    pathmatch:
      matching-strategy: ant_path_matcher

# Mybatis
mybatis:
  configuration:
    map-underscore-to-camel-case: true

# Logging
logging:
  level:
    root: debug
    web: debug
    sql: debug

# jwt
jwt:
  password: ssg4_jwt_password